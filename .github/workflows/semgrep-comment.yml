name: Semgrep Comment (reusable)

# Companion to semgrep.yml. Triggered in consumer repos by `workflow_run`
# after the Semgrep workflow completes. Runs in the *base repo* context with
# `pull-requests: write`, so it can post comments on fork PRs whose own scan
# ran with a read-only token.
#
# Trust posture (per Codex review round 1, P1 #1):
#   - PR number is derived from the trusted `workflow_run` event payload,
#     never from the artifact.
#   - Marker is hardcoded here (`## Semgrep Scan Results`), never from the
#     artifact.
#   - Body is read from artifact (rendered by trusted workflow code,
#     size-capped server-side).

on:
  workflow_call:

permissions:
  pull-requests: write
  actions: read

jobs:
  comment:
    name: Post Semgrep PR Comment
    runs-on: ubuntu-24.04
    if: github.event.workflow_run.event == 'pull_request' && github.event.workflow_run.conclusion != 'cancelled'

    steps:
      - name: Resolve PR number from workflow_run event
        id: pr
        env:
          PULL_REQUESTS_JSON: ${{ toJSON(github.event.workflow_run.pull_requests) }}
          HEAD_SHA: ${{ github.event.workflow_run.head_sha }}
          REPO: ${{ github.repository }}
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          n="$(printf '%s' "$PULL_REQUESTS_JSON" | jq -r '.[0].number // empty')"
          if [ -z "$n" ]; then
            if ! printf '%s' "$HEAD_SHA" | grep -qE '^[0-9a-f]{40}$'; then
              echo "::error::workflow_run.head_sha is not a 40-char SHA: '$HEAD_SHA'"
              exit 1
            fi
            n="$(gh api "repos/$REPO/pulls?state=open&per_page=100" \
              --jq ".[] | select(.head.sha == \"$HEAD_SHA\") | .number" \
              | head -n 1)"
          fi
          if ! printf '%s' "$n" | grep -qE '^[1-9][0-9]*$'; then
            echo '::error::Could not determine PR number from workflow_run event.'
            exit 1
          fi
          echo "number=$n" >> "$GITHUB_OUTPUT"

      - name: Post or update sticky PR comment
        uses: simple-container-com/actions/post-pr-comment@main
        with:
          workflow-run-id: ${{ github.event.workflow_run.id }}
          github-token: ${{ github.token }}
          pr-number: ${{ steps.pr.outputs.number }}
          artifact-name: pr-comment-body-semgrep
          marker: '## Semgrep Scan Results'
